import React from 'react';

import './Header.css';
import '../reset.css';

const Header = props => (
  <div id="header-container">
    <div id="header-title">collektiv</div>
  </div>
    );

export default Header;
